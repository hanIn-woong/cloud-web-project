# 구매 알고리즘 명세서

> 작성자: 한인웅  
> 작성일: 2026-06-16  
> ※ 실제 결제 시스템 없이 상태값 기반으로 거래를 표현합니다.

---

## 거래 상태 흐름

```
SALE (판매중)
  └─→ RESERVED (예약중)   ← 구매자가 예약 요청
        ├─→ SOLD (판매완료)  ← 판매자가 거래 완료 처리
        └─→ SALE (판매중)   ← 판매자가 예약 취소
```

---

## Book 도메인 추가 필드

```java
private String status;   // SALE(판매중) | RESERVED(예약중) | SOLD(판매완료)
private Long buyerId;    // 예약 또는 구매 확정한 회원 ID (없으면 null)
```

---

## API 명세

### 1. 구매 예약

구매자가 판매 중인 교재에 예약을 요청합니다.

| 항목 | 내용 |
|------|------|
| 메서드 | POST |
| 엔드포인트 | `/api/books/{id}/reserve` |
| 인증 | 🔒 필요 |

**처리 조건**
- 교재 상태가 `SALE`일 때만 예약 가능
- 본인이 등록한 교재는 예약 불가
- 예약 성공 시 상태 `SALE` → `RESERVED`, `buyerId` = 요청자 ID 로 변경

**성공 응답**
```json
{
  "success": true,
  "data": null,
  "message": null
}
```

**에러 응답 예시**
```json
{
  "success": false,
  "data": null,
  "message": "이미 예약된 교재입니다."
}
```
```json
{
  "success": false,
  "data": null,
  "message": "본인이 등록한 교재는 예약할 수 없습니다."
}
```

---

### 2. 예약 취소

판매자가 예약을 취소하고 다시 판매 상태로 되돌립니다.

| 항목 | 내용 |
|------|------|
| 메서드 | DELETE |
| 엔드포인트 | `/api/books/{id}/reserve` |
| 인증 | 🔒 필요 |

**처리 조건**
- 교재 상태가 `RESERVED`일 때만 취소 가능
- 해당 교재의 판매자만 취소 가능
- 취소 성공 시 상태 `RESERVED` → `SALE`, `buyerId` = `null` 로 변경

**에러 응답 예시**
```json
{
  "success": false,
  "data": null,
  "message": "예약 상태가 아닌 교재입니다."
}
```
```json
{
  "success": false,
  "data": null,
  "message": "판매자만 예약을 취소할 수 있습니다."
}
```

---

### 3. 거래 완료

판매자가 실제 거래가 완료됐음을 확정합니다.

| 항목 | 내용 |
|------|------|
| 메서드 | POST |
| 엔드포인트 | `/api/books/{id}/complete` |
| 인증 | 🔒 필요 |

**처리 조건**
- 교재 상태가 `RESERVED`일 때만 완료 처리 가능
- 해당 교재의 판매자만 완료 처리 가능
- 완료 성공 시 상태 `RESERVED` → `SOLD`

**에러 응답 예시**
```json
{
  "success": false,
  "data": null,
  "message": "예약 상태인 교재만 거래 완료 처리할 수 있습니다."
}
```

---

### 4. 구매 내역 조회

로그인한 사용자의 구매 완료 내역을 조회합니다.

| 항목 | 내용 |
|------|------|
| 메서드 | GET |
| 엔드포인트 | `/api/members/me/purchases` |
| 인증 | 🔒 필요 |

**응답 예시 (data)**
```json
[
  {
    "id": 3,
    "title": "데이터베이스 개론",
    "author": "이석호",
    "price": 15000,
    "status": "SOLD",
    "seller": "홍길동"
  }
]
```

---

## 상태별 허용 액션 정리

| 현재 상태 | 가능한 액션 | 주체 |
|----------|-----------|------|
| SALE | 예약 요청 | 구매자 |
| SALE | 교재 수정 / 삭제 | 판매자 |
| RESERVED | 예약 취소 | 판매자 |
| RESERVED | 거래 완료 | 판매자 |
| SOLD | 없음 (읽기만 가능) | - |

---

## 에러 케이스 요약

| 상황 | 상태코드 | 메시지 |
|------|---------|--------|
| 이미 예약된 교재 예약 시도 | 400 | 이미 예약된 교재입니다. |
| 본인 교재 예약 시도 | 400 | 본인이 등록한 교재는 예약할 수 없습니다. |
| 판매완료 교재 예약 시도 | 400 | 이미 판매 완료된 교재입니다. |
| 권한 없는 예약 취소 시도 | 400 | 판매자만 예약을 취소할 수 있습니다. |
| 권한 없는 거래완료 시도 | 400 | 판매자만 거래 완료 처리할 수 있습니다. |
